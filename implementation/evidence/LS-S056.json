{
  "slice": "LS-S056",
  "status": "implementationCompleteVerificationPending",
  "claimClass": "real-provider-development-smoke",
  "revision": "0856b2a",
  "checks": [
    {
      "command": "npm --prefix orchestration run runtime:check:mac",
      "result": "pass",
      "evidence": "Ollama, Moonshine STT, and VoxCPM2 TTS all returned exact pinned identities; Moonshine committed 'Local speech is ready.' and VoxCPM2 returned a completed PCM stream."
    },
    {
      "command": "authenticated POST /api/runtime/v1/tts",
      "result": "pass",
      "evidence": "The rebuilt Mac-local server streamed 34 non-fixture VoxCPM2 PCM data frames and a succeeded terminal with 161288 output samples."
    },
    {
      "command": "authenticated POST /api/runtime/v1/stt",
      "result": "pass",
      "evidence": "The rebuilt Mac-local server accepted a bounded 16 kHz PCM frame and returned a non-fixture Moonshine committed transcript with a succeeded terminal and 4800 input samples."
    },
    {
      "command": "browser surface and server tests",
      "result": "pass",
      "evidence": "Browser tests passed 2/2, focused server tests passed 5/5, and the full Lifestream suite passed 125/125."
    },
    {
      "command": "authenticated /api/runtime/v1/audio WebSocket",
      "result": "pass",
      "evidence": "A bounded live smoke against the rebuilt Mac-local server completed accepted session, committed Moonshine transcript, streamed Ollama response, 369 VoxCPM2 PCM chunks, and canonical completed terminal event."
    },
    {
      "command": "VAD and barge-in regression",
      "result": "pass",
      "evidence": "Server tests cover bounded PCM energy classification, silence suppression, downstream abort fencing, stopPlayback emission, and interrupted terminal state."
    },
    {
      "command": "npm --prefix orchestration run --silent runtime:check:mac (two consecutive runs)",
      "result": "pass",
      "evidence": "After deterministic MLX seeding and ten-step synthesis, both consecutive real VoxCPM2 outputs were recovered by real Moonshine as exactly 'Local speech is ready.'; each returned 69128 48 kHz samples in 15 frames."
    },
    {
      "command": "live browser and authenticated runtime profile checks at http://127.0.0.1:3910",
      "result": "pass",
      "evidence": "The browser exposed Mac-local and ai5090 selection, kept Mac-local active when all required ai5090 transports were unavailable, and displayed mac-local, qwen3.5:2b-q4_K_M, real after a streamed REAL_MODEL_CONFIRMED response. Browser TTS completed 34 scheduled PCM frames; final audible quality remains a Human observation."
    }
  ],
  "implemented": [
    "authenticated server-mediated bounded STT and TTS routes",
    "browser PCM capture, resampling, STT submission, and committed transcript display",
    "browser streaming PCM playback for selected TTS provider",
    "explicit Mac-local and ai5090 runtime profile selection with atomic ready-only switching",
    "visible real-versus-fixture inference provider and model identity",
    "canonical audio WebSocket upgrade and session transport",
    "bounded browser WebSocket capture, silence endpointing, and playback interruption",
    "truthful provider readiness and failure messaging"
  ],
  "excluded": [
    "physical microphone and audible-playback Human acceptance",
    "perceptual acceptance",
    "performance qualification",
    "production deployment"
  ]
}
