const confirmBox=document.querySelector('#confirm');const approve=document.querySelector('#approve');const result=document.querySelector('#result');confirmBox.addEventListener('change',()=>{approve.disabled=!confirmBox.checked});approve.addEventListener('click',()=>{result.textContent='Approval recorded by the server. Action completion will be reported separately.';approve.disabled=true;confirmBox.disabled=true});document.querySelector('#deny').addEventListener('click',()=>{result.textContent='Denial recorded by the server.';approve.disabled=true;confirmBox.disabled=true});
