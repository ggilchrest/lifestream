import assert from 'node:assert/strict';
import {test} from 'node:test';
import {randomUUID,randomBytes} from 'node:crypto';
import {mkdtemp,rm} from 'node:fs/promises';
import {tmpdir} from 'node:os';
import {join} from 'node:path';
import {createLifestreamServer} from '../../server/src/index.ts';
import {loadProfile} from '../../server/src/config/loader.ts';
test('rendered protected insight findings support explicit review, configuration activation and reversal', {skip:!process.env.PLAYWRIGHT_MODULE,timeout:60000},async t=>{
 const {chromium}=await import(process.env.PLAYWRIGHT_MODULE),root=await mkdtemp(join(tmpdir(),'ls-insight-browser-'));t.after(()=>rm(root,{recursive:true,force:true}));const config=loadProfile('test');config.authority.authentication='local-password';config.storage={databasePath:join(root,'db.sqlite'),artifactDirectory:join(root,'artifacts')};const installerToken=randomBytes(32).toString('hex'),app=createLifestreamServer({config,localAuth:{stateDirectory:join(root,'safety'),installerToken}});await app.start();t.after(()=>app.shutdown());const browser=await chromium.launch({channel:'chrome',headless:true});t.after(()=>browser.close());const page=await browser.newPage({viewport:{width:1360,height:960}});page.setDefaultTimeout(8000);const errors=[];page.on('pageerror',e=>errors.push(e.message));const waitText=(selector,text)=>page.waitForFunction(({selector,text})=>document.querySelector(selector)?.textContent.includes(text),{selector,text});
 const api=(path,body)=>page.evaluate(async({path,body})=>{const r=await fetch(path,{method:body===undefined?'GET':'POST',headers:{'content-type':'application/json','x-lifestream-csrf':window.lifestreamAuth.session?.csrfToken??''},...(body===undefined?{}:{body:JSON.stringify(body)})});return {status:r.status,body:await r.json()};},{path,body});
 await page.goto(`http://127.0.0.1:${app.address().port}/control/`);await page.locator('#auth-username').fill('owner');await page.locator('#auth-password').fill(randomBytes(32).toString('hex'));await page.locator('#auth-installer-token').fill(installerToken);await page.locator('#auth-setup').click();await page.waitForFunction(()=>window.lifestreamAuth.session);await page.locator('#new-assistant').click();await waitText('#result','Assistant created');const assistant=(await api('/api/admin/v1/assistants')).body.assistants[0];await page.locator('#relationship-create').click();await waitText('#relationship-results','revision 1');const relationship=(await api(`/api/admin/v1/assistants/${assistant.assistantId}/relationships`)).body.relationships[0],path=`/api/admin/v1/assistants/${assistant.assistantId}/relationships/${relationship.relationshipId}`;
 let revision=1;const add=async content=>{const candidate=(await api(path+'/candidates',{content,source:'authored',sourceFamily:'same-family',uncertainty:'high',expectedRevision:revision++,idempotencyKey:randomUUID()})).body.candidate;await api(path+`/candidates/${candidate.candidateId}/decision`,{decision:'approved',expectedRevision:revision++,idempotencyKey:randomUUID()});return candidate;};const first=await add('Use short explanations'),second=await add('Use short explanations');
 await page.locator('#relationship-insights-refresh').click();await waitText('#insight-feedback-target','Use short explanations');await page.locator('#insight-feedback-target').selectOption(first.candidateId);await page.locator('#insight-feedback-kind').selectOption('style');await page.locator('#insight-feedback-style').selectOption('preferBrief');await page.locator('#relationship-feedback-add').click();await waitText('#insight-feedback-history','independent support: no');await page.locator('#relationship-insights-run').click();await waitText('#insight-job','completed');await waitText('#relationship-insights-results','2 approved records across 1 attributed families');assert.match(await page.locator('#relationship-insights-results').textContent(),/1 exact normalized duplicate groups/u);
 const evidence=page.locator('[data-insight-id]').filter({has:page.getByRole('heading',{name:'Evidence Quality',exact:true})});await evidence.locator('summary').click();assert.match(await evidence.textContent(),/Counterevidence: none/u);await evidence.getByRole('button',{name:'Reject proposal',exact:true}).click();await waitText('#result','Review recorded');assert.match(await evidence.textContent(),/rejected/u);
 const style=page.locator('[data-insight-id]').filter({has:page.getByRole('heading',{name:'Communication Fit',exact:true})});await style.getByRole('button',{name:'Accept for Normal Review',exact:true}).click();await waitText('#relationship-insights-results','Prepared Configuration:');assert.equal((await api(path+'/configurations')).body.configurations.filter(c=>c.status==='active').length,0);await page.locator('#tuning-refresh').click();await waitText('#tuning-saved','Insight: reviewed communication preference');await page.locator('#tuning-preview').click();await page.locator('#relationship-config-activate').click();await waitText('#relationship-config-results','Effective: Insight: reviewed communication preference');assert.equal((await api(path+'/configurations')).body.configurations.find(c=>c.status==='active').controls.verbosity,0);
 await style.getByRole('button',{name:'Reverse Accepted Proposal',exact:true}).click();await waitText('#relationship-insights-results','Reversal Draft:');assert.equal((await api(path+'/configurations')).body.configurations.find(c=>c.status==='active').controls.verbosity,0);const rollback=(await api(path+'/configurations')).body.configurations.find(c=>c.name==='Reverse reviewed insight');await page.locator('#tuning-refresh').click();await page.locator('#tuning-saved').selectOption(rollback.configurationId);await page.locator('#tuning-preview').click();await page.locator('#relationship-config-activate').click();await waitText('#relationship-config-results','Effective: Reverse reviewed insight');assert.equal((await api(path+'/configurations')).body.configurations.find(c=>c.status==='active').controls.verbosity,0.5);
 await api(path+`/records/${first.candidateId}/operations`,{operation:'conflict',otherRecordId:second.candidateId,otherRecordRevision:2,disposition:'retain',expectedRevision:revision,expectedRecordRevision:2,idempotencyKey:randomUUID()});await page.locator('#relationship-insights-run').click();await waitText('#relationship-insights-results','2 records retain contradictions');await page.setViewportSize({width:390,height:844});await page.locator('#relationship-insights-refresh').click();await waitText('#insight-job','completed');assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1),true);await page.locator('#relationship-insights-results').scrollIntoViewIfNeeded();await page.screenshot({path:'/private/tmp/s077-responsive.png'});
 await page.locator('#new-assistant').click();await waitText('#result','Assistant created');assert.doesNotMatch(await page.locator('#relationship-insights-results').textContent(),/short explanations|attributed families/u);assert.deepEqual(errors,[]);
});
