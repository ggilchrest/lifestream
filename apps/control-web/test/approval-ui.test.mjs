import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {test} from 'node:test';
test('approval UI exposes reviewable terms and safe confirmation states',async()=>{const html=await readFile(new URL('../index.html',import.meta.url),'utf8');const css=await readFile(new URL('../styles.css',import.meta.url),'utf8');assert.match(html,/Effect/);assert.match(html,/Target/);assert.match(html,/Duration/);assert.match(html,/Action completion is reported separately/);assert.match(html,/type="checkbox"/);assert.match(css,/focus-visible/);assert.match(css,/@media/)});
