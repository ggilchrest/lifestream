export const PROTOCOL_VERSION = "voxcpm.loopback.v1" as const;
export type ProtocolRequest = { protocolVersion: typeof PROTOCOL_VERSION; requestId: string; correlationId: string; interactionId: string; deadlineAt: string; voiceBundleKey: string; voiceBundleRevision: number; text: string; delivery: Record<string, unknown>; format: { encoding: "pcm_s16le"; sampleRateHz: 48000; channels: 1 }; replay?: { seed: number; mode: "live" | "replay" } };
export type ProtocolEvent = { kind: "preAudio"; sequence: 0; requestId: string; correlationId: string; voiceBundleRevision: number; requestedDelivery: Record<string, unknown>; appliedDelivery: Record<string, unknown>; degradedDimensions: string[]; mappingRevision: string; effectiveSynthesis: Record<string, unknown>; format: ProtocolRequest["format"]; runtimeRevision: string; modelRevision: string } | { kind: "data"; sequence: number; sampleOffset: number; sampleCount: number; dataBase64: string; format: ProtocolRequest["format"] } | { kind: "terminal"; sequence: number; outcome: "completed" | "cancelled" | "deadlineExceeded" | "retryableProviderFailure" | "nonRetryableProviderFailure" | "malformedRequest" | "unsupportedRequest" | "providerUnavailable"; outputSamples: number; frameCount: number; errorCode?: string };
export function parseNdjson(body: string): ProtocolEvent[] { const lines = body.split("\n").map((line) => line.trim()).filter(Boolean); return lines.map((line) => JSON.parse(line) as ProtocolEvent); }
export function validateProtocolEvents(events: readonly ProtocolEvent[]): void { if (!events.length || events[0]?.kind !== "preAudio" || events.at(-1)?.kind !== "terminal") throw new Error("protocol lifecycle invalid"); events.forEach((event, index) => { if (event.sequence !== index) throw new Error("protocol sequence invalid"); if (event.kind === "data" && (event.sampleCount < 1 || event.sampleCount > 4800 || event.sampleOffset < 0 || !/^[A-Za-z0-9+/]+={0,2}$/.test(event.dataBase64))) throw new Error("protocol audio chunk invalid"); }); }
