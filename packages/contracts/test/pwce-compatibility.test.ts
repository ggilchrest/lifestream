import assert from "node:assert/strict"; import { test } from "node:test"; import { createPwceCompatibilityLock, validatePwceCompatibilityLock } from "../src/pwce/compatibility-lock.ts";
test("pins the published PWCE bundle and required core operations", () => { const lock = createPwceCompatibilityLock("adapter-revision"); validatePwceCompatibilityLock(lock); assert.equal(lock.pwceBundle.bundleDigest, "3af96275bc26754a8cebc64febb10b5ca50c951fd5003f106e06cfda846d4d3b"); assert.ok(lock.requiredOperations.includes("authority.authorizeDispatch")); assert.ok(lock.requiredOperations.includes("trace.publish")); });
test("rejects changed bundle digest, profile, client, and operation coverage", () => { const lock = createPwceCompatibilityLock("adapter-revision"); assert.throws(() => validatePwceCompatibilityLock({ ...lock, pwceBundle: { ...lock.pwceBundle, artifacts: [{ ...lock.pwceBundle.artifacts[0], sha256: "changed" }] } }), /digest/); assert.throws(() => validatePwceCompatibilityLock({ ...lock, pwceBundle: { ...lock.pwceBundle, generatedClientSha256: "changed" } }), /digest/); assert.throws(() => validatePwceCompatibilityLock({ ...lock, requiredOperations: ["context.query"] }), /operation/); assert.throws(() => validatePwceCompatibilityLock({ ...lock, lifestreamProfile: { ...lock.lifestreamProfile, profileVersion: "2.0.0" } }), /profile/); });
