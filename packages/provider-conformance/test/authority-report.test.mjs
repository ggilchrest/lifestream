import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {test}from'node:test';
test('authority composition and report label fixture-only evidence',async()=>{const source=await readFile(new URL('../src/authority/report.ts',import.meta.url),'utf8');assert.match(source,/fixtureOnly: true/);assert.match(source,/profile: "test"/);const composition=await readFile(new URL('../../../apps/server/src/composition/authority.ts',import.meta.url),'utf8');assert.match(composition,/dispatchExposed: false/);assert.match(composition,/denyByDefaultWhenUnavailable: true/)});
