export type CachedContext<T> = { value: T; dependencyRefs: string[]; stale: boolean };
export class ContextCache<T> { private readonly entries = new Map<string, CachedContext<T>>(); get(key: string): T | undefined { const entry = this.entries.get(key); return entry && !entry.stale ? entry.value : undefined; } set(key: string, value: T, dependencyRefs: string[] = []): void { this.entries.set(key, { value, dependencyRefs: [...new Set(dependencyRefs)], stale: false }); } invalidate(key: string): void { this.entries.delete(key); } invalidateDependencies(dependencyRefs: string[]): string[] { const targets = new Set(dependencyRefs); const invalidated: string[] = []; for (const [key, entry] of this.entries) if (entry.dependencyRefs.some((ref) => targets.has(ref))) { entry.stale = true; invalidated.push(key); } return invalidated; } clear(): void { this.entries.clear(); } }
